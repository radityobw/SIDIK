# GitHub Secret Scanning configuration
# Exclude test fixtures, synthetic benchmark datasets, and test suites containing dummy secrets

paths-ignore:
  - "dataset/**"
  - "tests/**"
  - "experiments/**"
